import styled from 'styled-components';
import * as vars from './Variables';

export const Button = styled.button`
  background-color: ${vars.text_secondary_colour};
  padding: 10px 20px;
  border-radius: 5px;
  font-weight: bold;
  color: ${vars.text_primary_colour};
`;
